# GPG Signing Server Dependencies

# Phase 1: Required for fetching GPG keys from AWS Secrets Manager at startup
boto3>=1.26.0

# Phase 2: Required for GitHub OIDC token validation (RS256)
# If not installed, server falls back to pre-shared token / JWT auth only
PyJWT[crypto]>=2.8.0
