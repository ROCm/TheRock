# Partially retrieved from https://github.com/pytorch/pytorch/blob/main/requirements.txt
# and https://github.com/pytorch/pytorch/blob/main/requirements-build.txt
pytest==9.0.3
pytest-timeout
pytest-xdist>=3.5.0
expecttest>=0.3.0
hypothesis
ninja
numpy
psutil
# Lets MultiProcessTestCase._dump_native_stack report C frames for a rank wedged
# inside a native call. faulthandler only walks Python frames, so without this
# the deepest thing a hung distributed rank can tell us is which Python line it
# entered native code from.
py-spy
