if(NOT TARGET cblas@SUFFIX64@)
  find_package(OpenBLAS@SUFFIX64@ REQUIRED)
  add_library(cblas@SUFFIX64@ INTERFACE)
  target_link_libraries(cblas@SUFFIX64@ INTERFACE OpenBLAS@SUFFIX64@::OpenBLAS)
  set(CBLAS_LIBRARIES cblas@SUFFIX64@)
endif()
