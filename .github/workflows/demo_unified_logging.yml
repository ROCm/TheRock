name: Demo - Unified Logging Framework

on:
  workflow_dispatch:
    inputs:
      artifact_run_id:
        description: 'Artifact Run ID to download binaries from'
        required: true
        default: '20151692873'
        type: string
      test_type:
        description: 'Test type'
        required: true
        default: 'smoke'
        type: choice
        options:
          - smoke
          - regression
          - full
  push:
    branches:
      - users/rponnuru/logging_poc_2
    paths:
      - 'build_tools/_therock_utils/logging_config.py'
      - 'build_tools/_therock_utils/test_runner.py'
      - 'build_tools/github_actions/test_executable_scripts/test_*.py'
      - '.github/workflows/demo_unified_logging.yml'

permissions:
  contents: read

jobs:
  demo_logging:
    name: 'Demo Unified Logging - ${{ matrix.component }}'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        component: [rocroller, rocwmma]
    
    env:
      # Use real artifacts from specified run ID
      ARTIFACT_RUN_ID: ${{ github.event.inputs.artifact_run_id || '20151692873' }}
      ARTIFACT_GROUP: "gfx94X-dcgpu"
      COMPONENT: ${{ matrix.component }}
      TEST_TYPE: ${{ github.event.inputs.test_type || 'smoke' }}
      VENV_DIR: ${{ github.workspace }}/.venv
      OUTPUT_ARTIFACTS_DIR: "./build"
      THEROCK_BIN_DIR: "./build/bin"
      THEROCK_BUILD_DIR: "./build"
      AMDGPU_FAMILIES: "gfx94X"
      RUNNER_OS: "Linux"
      SHARD_INDEX: "1"
      TOTAL_SHARDS: "1"
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: users/rponnuru/logging_poc_2
      
      - name: Display Logging Framework Info
        run: |
          echo "=========================================="
          echo "🎯 Unified Logging Framework Demo"
          echo "=========================================="
          echo ""
          echo "Component: $COMPONENT"
          echo "Test Type: $TEST_TYPE"
          echo "Artifact Run ID: $ARTIFACT_RUN_ID"
          echo "Artifact Group: $ARTIFACT_GROUP"
          echo ""
          echo "📋 Logging Features:"
          echo "  ✅ Centralized configuration"
          echo "  ✅ Single unified log format"
          echo "  ✅ Context-aware logging (component, operation)"
          echo "  ✅ Performance timing"
          echo "  ✅ Structured logging"
          echo "  ✅ Exception tracking"
          echo "  ✅ No GitHub Actions-specific code"
          echo ""
          echo "📁 Key Files:"
          echo "  • build_tools/_therock_utils/logging_config.py"
          echo "  • build_tools/_therock_utils/test_runner.py"
          echo "  • build_tools/github_actions/test_executable_scripts/test_${COMPONENT}.py"
          echo ""
          echo "=========================================="
      
      - name: Show Logging Configuration
        run: |
          echo "📝 Current Logging Configuration:"
          echo ""
          grep -A 1 "^LOG_FORMAT" build_tools/_therock_utils/logging_config.py || true
          echo ""
          echo "Log Format: timestamp - logger_name - level - message"
          echo "Log Level: INFO (controlled via configure_root_logger)"
          echo ""
      
      - name: Download Real Artifacts
        uses: './.github/actions/setup_test_environment'
        with:
          ARTIFACT_RUN_ID: ${{ env.ARTIFACT_RUN_ID }}
          ARTIFACT_GROUP: ${{ env.ARTIFACT_GROUP }}
          OUTPUT_ARTIFACTS_DIR: ${{ env.OUTPUT_ARTIFACTS_DIR }}
          VENV_DIR: ${{ env.VENV_DIR }}
          FETCH_ARTIFACT_ARGS: ${{ matrix.component == 'rocroller' && '--blas --tests' || '--tests' }}
          IS_PR_FROM_FORK: false
      
      - name: Run ${{ matrix.component }} Test with Unified Logging
        run: |
          echo ""
          echo "=========================================="
          echo "🚀 Running $COMPONENT Tests with Unified Logging"
          echo "=========================================="
          echo ""
          
          # Run the test script which uses unified logging framework
          python3 build_tools/github_actions/test_executable_scripts/test_${COMPONENT}.py
      
      - name: Demo Summary
        if: always()
        run: |
          echo ""
          echo "=========================================="
          echo "📊 Demo Summary - $COMPONENT"
          echo "=========================================="
          echo ""
          echo "✅ Demonstrated Features:"
          echo ""
          echo "1️⃣  Real Test Execution"
          echo "   • Downloaded artifacts from Run ID: $ARTIFACT_RUN_ID"
          echo "   • Ran actual $COMPONENT tests (not mocks)"
          echo "   • Used real GTest/CTest binaries"
          echo ""
          echo "2️⃣  Unified Logging Configuration"
          echo "   • Single LOG_FORMAT across all outputs"
          echo "   • Consistent: timestamp - logger - level - message"
          echo "   • No environment-specific formatting"
          echo ""
          echo "3️⃣  TestRunner Integration"
          echo "   • Captures GTest/CTest output"
          echo "   • Parses test results"
          echo "   • Structured logging with context"
          echo "   • Performance timing built-in"
          echo ""
          echo "4️⃣  Log Output Features"
          echo "   • Component tracking ($COMPONENT)"
          echo "   • Operation timing with timed_operation()"
          echo "   • Test result summaries"
          echo "   • Exception logging"
          echo ""
          echo "5️⃣  Simplicity & Consistency"
          echo "   • No GitHub Actions-specific code"
          echo "   • No JSON formatting"
          echo "   • No threading locks"
          echo "   • Works same locally and in CI"
          echo ""
          echo "📁 Key Files:"
          echo "   • logging_config.py - Core framework (~400 lines, simplified)"
          echo "   • test_runner.py - Test execution wrapper"
          echo "   • test_${COMPONENT}.py - Component test script"
          echo ""
          echo "=========================================="

