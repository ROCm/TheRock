name: Publish no_rocm_image_ubuntu24_04_runtime images
on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'stage/docker/**'
    paths:
      - dockerfiles/no_rocm_image_ubuntu24_04_runtime.Dockerfile
      - .github/workflows/publish_no_rocm_image_ubuntu24_04_runtime.yml

permissions:
  contents: read
  packages: write

jobs:
  publish_no_rocm_image_ubuntu24_04_rocgdb:
    uses: ./.github/workflows/publish_dockerfile.yml
    with:
      DOCKER_FILE_NAME: no_rocm_image_ubuntu24_04_runtime
      DOCKER_IMAGE_NAME: no_rocm_image_ubuntu24_04_runtime
