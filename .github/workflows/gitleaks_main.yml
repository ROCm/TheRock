# Copyright Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT
#
# Post-merge gitleaks scan: runs after every push to main
# and pushes SARIF findings to the repository's code-scanning
# Security tab. TBD if we use post merge or schedule.
name: Gitleaks_main

on:
  push:
    branches: [main]
  # Allow re-running the post-merge scan on demand, e.g. after
  # tweaking `gitleaks.toml` or rotating the gitleaks version pin, so
  # the Security tab can be refreshed without an unrelated commit.
  workflow_dispatch:

jobs:
  gitleaks:
    uses: ./.github/workflows/gitleaks.yml
    with:
      scan_mode: all
      report_formats: sarif
    permissions:
      contents: read
      # Required so the reusable workflow can call
      # github/codeql-action/upload-sarif and have the findings
      # appear under Security -> Code scanning, filterable by
      # `Tool: gitleaks` separately from any other scanner.
      security-events: write
