name: Test Packages

on:
  workflow_dispatch:
    inputs:
      artifact_run_id:
        type: string
        default: ""
      amdgpu_families:
        type: string
      test_runs_on:
        type: string
      platform:
        type: string
  workflow_call:
    inputs:
      artifact_run_id:
        type: string
        default: ""
      amdgpu_families:
        type: string
      test_runs_on:
        type: string
      platform:
        type: string
  push:
    branches:
      - ADHOCBUILD

jobs:
  test_sanity_check:
    name: 'Test Sanity Check'
    if: ${{ inputs.test_runs_on != '' }} # if there is a test machine available
    uses: './.github/workflows/test_sanity_check.yml'
    with:
      artifact_run_id: ${{ inputs.artifact_run_id }}
      amdgpu_families: ${{ inputs.amdgpu_families }}
      test_runs_on: ${{ inputs.test_runs_on }}
      platform: ${{ inputs.platform }}

  configure_test_matrix:
    name: "Configure test matrix"
    needs: test_sanity_check
    runs-on: ubuntu-24.04
    outputs:
      components: ${{ steps.configure.outputs.components }}
    steps:
      - name: "Checking out repository"
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: "Configuring CI options"
        env:
          THEROCK_BIN_DIR: ${{ github.workspace }}/build/bin
          PLATFORM: ${{ inputs.platform }}
        id: configure
        run: ./build_tools/github_actions/fetch_test_configurations.py

  test_libs:
    name: 'Test ${{ matrix.components.name }}'
    runs-on: ${{ inputs.test_runs_on }}
    needs: configure_test_matrix
    strategy:
      fail-fast: false
      matrix:
        components: ${{ fromJSON(needs.configure_test_matrix.outputs.components) }}
    defaults:
      run:
        shell: bash
    env:
      VENV_DIR: ${{ github.workspace }}/.venv
      ARTIFACT_RUN_ID: "${{ inputs.artifact_run_id != '' && inputs.artifact_run_id || github.run_id }}"
      OUTPUT_ARTIFACTS_DIR: ${{ github.workspace }}/build
      THEROCK_BIN_DIR: ${{ github.workspace }}/build/bin
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: "ROCm/TheRock"

      - name: Run setup test environment workflow
        uses: './.github/actions/setup_test_environment'
        with:
          ARTIFACT_RUN_ID: ${{ env.ARTIFACT_RUN_ID }}
          AMDGPU_FAMILIES: ${{ inputs.amdgpu_families }}
          OUTPUT_ARTIFACTS_DIR: ${{ env.OUTPUT_ARTIFACTS_DIR }}
          VENV_DIR: ${{ env.VENV_DIR }}
          FETCH_ARTIFACT_ARGS: ${{ matrix.components.artifact_flags }}
          PLATFORM: ${{ inputs.platform }}

      - name: Install additional packages
        if: ${{ inputs.platform == 'linux' && (matrix.components.name == 'rocblas' || matrix.components.name == 'hipblaslt') }}
        run: sudo apt install libgfortran5 -y

      - name: Run ${{ matrix.components.name }} tests
        timeout-minutes: ${{ matrix.components.timeout }}
        run: |
          if [ "${{ inputs.platform }}" == "linux" ]; then source ${VENV_DIR}/bin/activate ; else . ${VENV_DIR}/Scripts/activate ; fi
          ${{ fromJSON(matrix.components.executable_command) }}
